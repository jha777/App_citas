specVersion: "v1beta"
serviceId: "app_citas"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "app_citas-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
